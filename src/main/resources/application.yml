
spring:
  datasource:
    url: jdbc:mysql://localhost:3307/dbspotmusic?useSSL=false&createDatabaseIfNotExist=true
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    properties:
      show_sql: true
      hibernate:
      hbm2ddl:
      auto: update